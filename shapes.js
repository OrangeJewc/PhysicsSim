function Square(x, y, width) {
	this.x = x;
	this.y = y;
	this.width = width;
}

function Circle(x, y, radius) {
	this.x = x;
	this.y = y;
	this.radius = radius;
}

function Triangle(x, y, side) {
	this.x = x;
	this.y = y;
	this.side = side;
}

